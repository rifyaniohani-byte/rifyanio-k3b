rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write for authenticated operations or system maritime workflow
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
